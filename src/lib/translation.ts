const translations: Map<string, Map<string, string>> = new Map([
    ["en", new Map([
        ["home.desc", "fdsa"]
    ])],
    ["kr", new Map([
        ["home.desc", "hangul"]
    ])]
])

export default translations